% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{dmft_config}
\alias{dmft_config}
\title{Create a DMFT analysis configuration}
\usage{
dmft_config(
  regions,
  region_col = "province",
  year_range = c(1990, 2025),
  projection_range = NULL,
  age_groups_deciduous = c("0-4", "5-9", "10-14"),
  age_groups_permanent = c("5-9", "10-14", "15-19", "20-24", "25-29", "30-34", "35-39",
    "40-44", "45-49", "50-54", "55-59", "60+"),
  dmft_max_deciduous = 20L,
  dmft_max_permanent = 28L,
  cv_default = 0.3,
  covariates = NULL,
  ast_params = list(par_space = 0.9, par_time = 2, par_age = 1, weight_coverage = 0.9),
  n_boot = 1000L,
  stan_settings = NULL,
  seed = 12345L,
  region_mapping = NULL
)
}
\arguments{
\item{regions}{Character vector of region names (e.g., provinces).}

\item{region_col}{Column name in the data containing region names.}

\item{year_range}{Length-2 integer vector \code{c(start, end)} for historical period.}

\item{projection_range}{Optional length-2 vector \code{c(start, end)} for projections.
If \code{NULL}, no projections are configured.}

\item{age_groups_deciduous}{Character vector of deciduous age group labels.}

\item{age_groups_permanent}{Character vector of permanent age group labels.}

\item{dmft_max_deciduous}{Maximum biologically plausible dmft (default 20).}

\item{dmft_max_permanent}{Maximum biologically plausible DMFT (default 28).}

\item{cv_default}{Default coefficient of variation for uncertainty imputation.}

\item{covariates}{Character vector of covariate column names to include as
fixed effects in the mixed model. If \code{NULL}, only the intercept is used.}

\item{ast_params}{Named list of AST smoothing parameters:
\describe{
\item{par_space}{Spatial correlation parameter (default 0.9).}
\item{par_time}{Temporal smoothing parameter, lambda (default 2).}
\item{par_age}{Age smoothing parameter, omega (default 1).}
\item{weight_coverage}{Weight for high-coverage data sources (default 0.9).}
}}

\item{n_boot}{Number of bootstrap replicates for uncertainty intervals.}

\item{stan_settings}{Optional named list of Stan sampling settings for the
experimental Bayesian backend (see \code{\link[=dmft_fit_bayes]{dmft_fit_bayes()}}). Elements:
\code{chains}, \code{iter_warmup}, \code{iter_sampling}, \code{adapt_delta}, \code{max_treedepth}.
If \code{NULL}, sensible defaults are used.}

\item{seed}{Random seed for reproducibility.}

\item{region_mapping}{Optional named list mapping alternative region names to
canonical names in \code{regions}.}
}
\value{
An object of class \code{"dmft_config"}.
}
\description{
Defines all parameters for a DMFT/dmft analysis: regions, time period,
age groups, AST smoothing parameters, and projection settings.
}
\examples{
cfg <- dmft_config(
  regions = c("Region_A", "Region_B", "Region_C"),
  region_col = "province",
  year_range = c(2000, 2020)
)
cfg
}
