% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fit_bayes.R
\name{dmft_fit_bayes}
\alias{dmft_fit_bayes}
\title{Fit a Bayesian random intercept model for DMFT/dmft via cmdstanr}
\usage{
dmft_fit_bayes(
  data,
  adjacency,
  dentition = c("deciduous", "permanent"),
  config,
  stan_settings = NULL
)
}
\arguments{
\item{data}{A data frame (one of the dentition subsets from \code{\link[=dmft_clean]{dmft_clean()}}).}

\item{adjacency}{Adjacency object from \code{\link[=dmft_adjacency]{dmft_adjacency()}}.}

\item{dentition}{\code{"deciduous"} or \code{"permanent"}.}

\item{config}{A \link{dmft_config} object.}

\item{stan_settings}{Named list overriding default Stan sampling settings:
\code{chains}, \code{iter_warmup}, \code{iter_sampling}, \code{adapt_delta}, \code{max_treedepth}.
If \code{NULL}, uses defaults from config or built-in defaults.}
}
\value{
A list of class \code{"dmft_fit_bayes"} with elements \code{fit} (CmdStanMCMC),
\code{mdata} (prepared model data), \code{config}, and \code{dentition}.
}
\description{
Experimental Bayesian alternative to \code{\link[=dmft_fit]{dmft_fit()}}. Uses the same
random intercept structure (\code{y ~ 1 + covariates + (1|region) + (1|year)})
but estimates parameters via Hamiltonian Monte Carlo (Stan) rather than
REML. AST smoothing (Stage 2) is applied downstream via \code{\link[=dmft_predict_bayes]{dmft_predict_bayes()}}.
}
